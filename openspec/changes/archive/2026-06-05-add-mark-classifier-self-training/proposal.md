# add-mark-classifier-self-training

Add a lightweight checkbox mark classifier and self-training loop: geometry crop providers, labeled crop
harvesting, deterministic model training/export, precision-safe threshold selection, and optional
PaddleOCR plugin classifier integration.
